import React, { useState, useEffect } from 'react';

export default function InfiniteDisplay(props) {
    return <div>Hi</div>;
}

// Scan, 0, 0, R, Scan
// Scan, 1, 1, R, Scan
// Scan, #, 0, R, Adding
// Adding, #, 0, R, Halt
